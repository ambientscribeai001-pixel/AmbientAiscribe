# ─── AmbientScribe Backend · Environment Template ────────────────────────────
# Copy this file to .env. Fill EVERY value directly in Railway's dashboard
# (Variables tab) or in your local .env — NEVER paste real keys into chat,
# screenshots, or commit them to git. .env is already in .gitignore.

PORT=5000
NODE_ENV=production
ALLOWED_ORIGINS=https://PASTE_YOUR_NETLIFY_URL_HERE.netlify.app

# ── MongoDB Atlas ─────────────────────────────────────────────────────────────
MONGO_URI=PASTE_YOUR_MONGO_ATLAS_CONNECTION_STRING_HERE

# ── JWT ───────────────────────────────────────────────────────────────────────
# Generate locally with:
#   node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=PASTE_YOUR_GENERATED_64_BYTE_HEX_STRING_HERE
JWT_EXPIRES_IN=24h
BCRYPT_ROUNDS=12

# ── AI Provider ───────────────────────────────────────────────────────────────
# Currently using Gemini (Google AI Studio) — free tier while budget is tight.
# Switch to 'claude' later by changing AI_PROVIDER + adding ANTHROPIC_API_KEY.
AI_PROVIDER=gemini

# Google AI Studio key — https://aistudio.google.com/app/apikey
GEMINI_API_KEY=PASTE_YOUR_GEMINI_KEY_HERE
GEMINI_MODEL=gemini-2.0-flash

# Future — when ready to switch back to Claude:
# AI_PROVIDER=claude
# ANTHROPIC_API_KEY=PASTE_YOUR_ANTHROPIC_KEY_HERE

# ── Paystack ──────────────────────────────────────────────────────────────────
# Already partially activated — supports card, bank transfer, USSD, mobile money
PAYSTACK_SECRET_KEY=PASTE_YOUR_PAYSTACK_SECRET_KEY_HERE
PAYSTACK_PUBLIC_KEY=PASTE_YOUR_PAYSTACK_PUBLIC_KEY_HERE
PAYSTACK_CURRENCY=NGN

# Plan codes — already created in your Paystack dashboard
PAYSTACK_PRO_MONTHLY_PLAN_CODE=PLN_bhwojzlyqlcjwpu
PAYSTACK_CLINIC_MONTHLY_PLAN_CODE=PLN_e37xlb078c3t1y4
PAYSTACK_ENTERPRISE_MONTHLY_PLAN_CODE=PLN_4udww3kyncpzxaz

# Annual plan codes — create in dashboard when ready (20% discount)
PAYSTACK_PRO_ANNUAL_PLAN_CODE=
PAYSTACK_CLINIC_ANNUAL_PLAN_CODE=
PAYSTACK_ENTERPRISE_ANNUAL_PLAN_CODE=

# Where Paystack redirects after checkout (your Netlify URL)
PAYSTACK_CALLBACK_URL=https://PASTE_YOUR_NETLIFY_URL_HERE.netlify.app/payment/callback

# Where Paystack sends webhook events (your Railway backend URL)
# Register this exact URL in Paystack Dashboard → Settings → Webhooks
PAYSTACK_WEBHOOK_URL=https://PASTE_YOUR_RAILWAY_URL_HERE.up.railway.app/api/v1/webhooks/paystack

# ── Lemon Squeezy ────────────────────────────────────────────────────────────
# Add once your Lemon Squeezy store is approved
LEMONSQUEEZY_API_KEY=PASTE_WHEN_READY
LEMONSQUEEZY_STORE_ID=PASTE_WHEN_READY
LEMONSQUEEZY_WEBHOOK_SECRET=PASTE_WHEN_READY
