# ─── AmbientScribe Backend · Environment Template ────────────────────────────
# Copy this file to .env and fill in real values.
# NEVER commit .env to git. Add it to .gitignore immediately.

PORT=5000
NODE_ENV=development

# MongoDB Atlas connection string
# Replace <username>, <password>, and <cluster> with your Atlas values
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/AmbientScribeProduction?retryWrites=true&w=majority&appName=Cluster0

# JWT signing secret — generate with:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=replace_with_64_byte_hex_string_generated_from_crypto

# Token expiry (24h for enterprise sessions)
JWT_EXPIRES_IN=24h

# bcrypt work factor (12 = ~250ms hash time on modern hardware, good balance)
BCRYPT_ROUNDS=12
