# ─── AmbientScribe Backend · Environment Template ────────────────────────────
# Copy to .env — NEVER commit .env to git

PORT=5000
NODE_ENV=development
ALLOWED_ORIGINS=http://localhost:3000

# MongoDB Atlas
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/AmbientScribeProduction?retryWrites=true&w=majority

# JWT — generate: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=replace_with_64_byte_hex
JWT_EXPIRES_IN=24h
BCRYPT_ROUNDS=12

# ── Paystack ─────────────────────────────────────────────────────────────────
PAYSTACK_SECRET_KEY=sk_live_xxxxxxxxxxxxxxxxxxxx
PAYSTACK_PUBLIC_KEY=pk_live_xxxxxxxxxxxxxxxxxxxx

# Create these in Paystack dashboard → Subscriptions → Plans
# Amounts are set IN the Paystack dashboard, not here.
#
# Clinical Pro  → $300/month | $240/month (annual)
PAYSTACK_PRO_MONTHLY_PLAN_CODE=PLN_xxxxxxxxxx
PAYSTACK_PRO_ANNUAL_PLAN_CODE=PLN_xxxxxxxxxx

# Clinic        → $400/month | $320/month (annual)
PAYSTACK_CLINIC_MONTHLY_PLAN_CODE=PLN_xxxxxxxxxx
PAYSTACK_CLINIC_ANNUAL_PLAN_CODE=PLN_xxxxxxxxxx

# Enterprise    → $450/month (annual contracts negotiated separately)
PAYSTACK_ENTERPRISE_MONTHLY_PLAN_CODE=PLN_xxxxxxxxxx
PAYSTACK_ENTERPRISE_ANNUAL_PLAN_CODE=PLN_xxxxxxxxxx

# Paystack callback after checkout (your deployed frontend URL)
PAYSTACK_CALLBACK_URL=https://your-app.com/payment/callback
